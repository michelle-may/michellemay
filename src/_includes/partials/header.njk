<header class="masthead flex">

    {% if page.url === '/' %}
    <h1 class="masthead__logo" href="/" title="{{ site.name }}">
      {% include "partials/logo.njk" %}
      Michelle May
    </h1>
    {% else %}
		<a class="masthead__logo" href="/" title="{{ site.name }}">
      {% include "partials/logo.njk" %}
      Michelle May
    </a>
    {% endif %}

    <nav class="main-nav">
      <ul class="main-nav__list flex">
        {% for item in nav.items %}
        <li class="main-nav__list-item">
          <a href="{{ item.url }}">{{ item.label }}</a>
        </li>
        {% endfor %}
      </ul>
    </nav>

</header>
